name: CpsCounter
main: net.infnetwork.snowball.cpscounter.CpsCounter
version: ${project.version}
author: SakuraKooi
# 1.21.11 移植版。必须声明 api-version,否则 Paper 按 legacy 模式加载。
api-version: '1.21'
commands:
  cps:
    description: get other player cps
    usage: /cps
permissions:
  cpscounter.cps:
    default: op
  cpscounter.bypass:
    default: op